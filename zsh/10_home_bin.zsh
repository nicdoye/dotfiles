########################################################################
# Non-Homebrew
unix_home=${HOME}/unix
opt_prefix=${unix_home}/opt
########################################################################
export PATH=${unix_home}/bin:${unix_home}/sbin:${PATH}
